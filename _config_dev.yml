# Development overrides
# Use with: jekyll serve --config _config.yml,_config_dev.yml

exclude:
  - Gemfile
  - Gemfile.lock
  - node_modules
  - vendor/bundle/
  - vendor/cache/
  - vendor/gems/
  - vendor/ruby/
